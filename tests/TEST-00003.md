# Test Cases - FEAT-00003

> Stdio MCP client lifecycle (`internal/mcpclient`).
> Derived from `docs/business-logic.md` §3/§5 + `docs/tdd/MCP_HOST_TDD.md` §5, §8, AC1/AC4/AC5.
> Status: ✅ Pass · ❌ Fail · ⏭️ Skip. Execution evidence in `tests/REPORTS.md`.

## TEST-00020: Happy path — initialize, list, call (mcpclient integration)

| Field | Value |
|---|---|
| Target | FEAT-00003 - `internal/mcpclient` lifecycle |
| Type | Integration |
| Objective | AC1 + AC3 surface: a healthy child completes `initialize`, `tools/list` returns tools, `tools/call` returns content. |
| Precondition | Fake stdio server built from `testdata/fakeserver` in `normal` mode; `MCP_TIMEOUT=5s`. |
| Steps | 1. `Initialize(ctx)`. 2. `ListTools(ctx)`. 3. `CallTool(ctx,"echo",{message:"hi"})`. |
| Expected | No errors; 1 tool named `echo`; call content text `ok`; `IsError==false`. |
| Actual | All three operations succeeded; tool `echo`; content `ok`. |
| Status | ✅ PASS |

## TEST-00021: NewStdio rejects nil policy and non-positive timeout (mcpclient boundary)

| Field | Value |
|---|---|
| Target | FEAT-00003 - `internal/mcpclient` constructor |
| Type | Unit |
| Objective | Boundary: `NewStdio(nil, ...)` and `NewStdio(p, 0)` must error. |
| Precondition | `p` is a valid policy; timeout `0`. |
| Steps | 1. `NewStdio(nil, time.Second)`. 2. `NewStdio(p, 0)`. |
| Expected | Both return an error. |
| Actual | Both returned an error. |
| Status | ✅ PASS |

## TEST-00022: Child crash returns ErrProcessExit, Close idempotent (mcpclient negative)

| Field | Value |
|---|---|
| Target | FEAT-00003 - AC4 crash recovery |
| Type | Integration |
| Objective | A child that exits(1) yields a typed `ErrProcessExit`; `Close` remains safe and idempotent. |
| Precondition | Fake server in `crash` mode. |
| Steps | 1. `Initialize`. 2. `Close()` twice. |
| Expected | `errors.Is(err, ErrProcessExit)==true`; second Close returns nil (idempotent). |
| Actual | ErrProcessExit returned; second Close nil. |
| Status | ✅ PASS |

## TEST-00023: Never-responding child returns ErrTimeout (mcpclient boundary)

| Field | Value |
|---|---|
| Target | FEAT-00003 - MCP_TIMEOUT enforcement |
| Type | Integration |
| Objective | A child that never responds triggers `ErrTimeout` within `MCP_TIMEOUT` (200ms); host remains usable. |
| Precondition | Fake server in `timeout` mode; timeout `200ms`. |
| Steps | 1. `Initialize`. 2. `Close()`. |
| Expected | `errors.Is(err, ErrTimeout)==true`; Close after timeout returns nil. |
| Actual | ErrTimeout returned; Close clean. |
| Status | ✅ PASS |

## TEST-00024: Server protocol error returns ErrInvalidResponse (mcpclient negative)

| Field | Value |
|---|---|
| Target | FEAT-00003 - protocol fault handling |
| Type | Integration |
| Objective | A JSON-RPC error payload for `tools/call` surfaces as typed `ErrInvalidResponse`, not a crash. |
| Precondition | Fake server in `error` mode; `initialize` succeeds. |
| Steps | 1. `Initialize`. 2. `CallTool("echo", {})`. |
| Expected | `errors.Is(err, ErrInvalidResponse)==true`. |
| Actual | ErrInvalidResponse returned. |
| Status | ✅ PASS |

## TEST-00025: Close terminates child — no orphan (mcpclient positive)

| Field | Value |
|---|---|
| Target | FEAT-00003 - AC4 no-leak cleanup |
| Type | Integration |
| Objective | `Close` delivers stdin EOF so the child writes an exit marker within 3s — proving termination, no orphan. |
| Precondition | Fake server in `timeout` mode with a root arg; Initialize returns ErrTimeout. |
| Steps | 1. `Close()`. 2. Poll for `<root>/exit-marker.txt`. |
| Expected | Marker file appears within 3s. |
| Actual | Marker written; child terminated cleanly. |
| Status | ✅ PASS |

## TEST-00026: Typed error unwraps to sentinel (mcpclient unit)

| Field | Value |
|---|---|
| Target | FEAT-00003 - error taxonomy |
| Type | Unit |
| Objective | Each Kind unwraps to its sentinel via `errors.Is`, and never to `ErrTimeout`. |
| Precondition | `newError` for each Kind. |
| Steps | 1. Assert `errors.Is(err, sentinel)`. 2. Assert NOT `ErrTimeout`. |
| Expected | All 5 sentinels match; ErrTimeout never matches. |
| Actual | All matched correctly. |
| Status | ✅ PASS |

## TEST-00027: Error messages are user-safe (mcpclient security)

| Field | Value |
|---|---|
| Target | FEAT-00003 - secret/path redaction |
| Type | Unit |
| Objective | Typed messages contain no backslash (host path), no `secret`/`token` (credential), and name the operation. |
| Precondition | `newError(KindCallFailed, "tools/call")`. |
| Steps | 1. Inspect `err.Error()`. |
| Expected | Non-empty; no `\`; no `secret`/`token`; `TimeoutError` names operation. |
| Actual | All assertions held. |
| Status | ✅ PASS |
# Test Cases - FEAT-00001

## TEST-00001: Approved SDK baseline resolves

| Field | Value |
|---|---|
| Target | FEAT-00001 - Go module dependency baseline |
| Type | Integration |
| Objective | Verify the approved Google Gen AI SDK is present in the module graph. |
| Precondition | Go toolchain is available from the repository root. |
| Steps | 1. Run `go list -m google.golang.org/genai`. |
| Expected | The command reports `google.golang.org/genai v1.66.0`. |
| Actual | `google.golang.org/genai v1.66.0` returned. |
| Status | Pass |
| Evidence | `tests/REPORTS.md#test-00001` |

## TEST-00002: Bootstrap does not disclose a credential

| Field | Value |
|---|---|
| Target | FEAT-00001 - CLI composition root |
| Type | E2E |
| Objective | Verify the bootstrap executable emits its fixed safe status when a representative Gemini credential is present. |
| Precondition | `GEMINI_API_KEY` is set to a non-secret test sentinel for the command process. |
| Steps | 1. Set `GEMINI_API_KEY` to `test-secret`. 2. Run `go run ./cmd/server`. |
| Expected | Stdout is exactly `MCP Host bootstrap complete. Integrations are not configured or started.` and does not contain the sentinel. |
| Actual | The exact expected status was returned and the sentinel was absent. |
| Status | Pass |
| Evidence | `tests/REPORTS.md#test-00002` |

## TEST-00003: Later-feature configuration remains inert at bootstrap

| Field | Value |
|---|---|
| Target | FEAT-00001 - scope boundary |
| Type | E2E |
| Objective | Verify configuration variables owned by later tasks do not make bootstrap claim an initialized MCP or Gemini integration. |
| Precondition | The command process has representative MCP and Gemini configuration values. |
| Steps | 1. Set `GEMINI_MODEL`, `MCP_FILESYSTEM_COMMAND`, and `MCP_ALLOWED_ROOTS`. 2. Run `go run ./cmd/server`. |
| Expected | Stdout remains the exact bootstrap-only status and reports no initialized integration. |
| Actual | The exact expected bootstrap-only status was returned. |
| Status | Pass |
| Evidence | `tests/REPORTS.md#test-00003` |

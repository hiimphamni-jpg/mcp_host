# Test Cases - FEAT-00004

> MCP JSON Schema → Gemini function-declaration mapper (`internal/mapping`).
> Derived from `docs/business-logic.md` §6 + `docs/tdd/MCP_HOST_TDD.md` §6, AC2.
> Status: ✅ Pass · ❌ Fail · ⏭️ Skip. Execution evidence in `tests/REPORTS.md`.

## TEST-00028: Full supported subset mapped losslessly (mapping positive)

| Field | Value |
|---|---|
| Target | FEAT-00004 - AC2 lossless mapping |
| Type | Unit |
| Objective | A schema using every MVP construct (string, integer, number, boolean, array+items, enum `[]any`+`[]string`, nested object, nested/root `required`) maps to the exact expected declaration. |
| Precondition | `MapTools([file_ops])` with full-featured props. |
| Steps | 1. Call `MapTools`. |
| Expected | No error; 1 declaration; `reflect.DeepEqual` matches expected `genai.FunctionDeclaration` (names, types, enums, nested required). |
| Actual | Matches expected declaration exactly. |
| Status | ✅ PASS |

## TEST-00029: No-argument tool maps to empty schema (mapping boundary)

| Field | Value |
|---|---|
| Target | FEAT-00004 - empty input |
| Type | Unit |
| Objective | Boundary: a tool with no properties yields empty, non-nil Parameters.Properties. |
| Precondition | Tool `ping` with `map[string]any{}` props. |
| Steps | 1. Call `MapTools`. |
| Expected | 1 declaration; Parameters.Properties is non-nil and length 0. |
| Actual | Empty non-nil Parameters.Properties. |
| Status | ✅ PASS |

## TEST-00030: Unsupported schema type rejected (mapping negative)

| Field | Value |
|---|---|
| Target | FEAT-00004 - type whitelist |
| Type | Unit |
| Objective | Negative: types outside the subset (`null`, `any`, empty, missing, unknown) are rejected with `ErrUnsupportedType`/`ErrMalformed` and the tool name in the message. |
| Precondition | 5 cases: type null/any/empty/""/undefined, plus missing type. |
| Steps | 1. For each, call `MapTools`. |
| Expected | Error returned; `errors.Is(ErrUnsupportedType or ErrMalformed)`; message contains `my_tool` + field name. |
| Actual | All rejected; `errors.Is` matches; tool+field in message. |
| Status | ✅ PASS |

## TEST-00031: Nested unsupported type carries full path (mapping boundary)

| Field | Value |
|---|---|
| Target | FEAT-00004 - field-path errors |
| Type | Unit |
| Objective | Boundary: an unsupported type deep inside nested objects reports the dot-delimited path (`config.inner.x`). |
| Precondition | `config → inner → x` where x has `type:"void"`. |
| Steps | 1. Call `MapTools`. |
| Expected | `ErrUnsupportedType`; message contains `myTool`, `config`, `inner`, `x`. |
| Actual | Error contains tool name and all path segments. |
| Status | ✅ PASS |

## TEST-00032: Malformed schema rejected (mapping negative)

| Field | Value |
|---|---|
| Target | FEAT-00004 - structural validation |
| Type | Unit |
| Objective | Negative: property-not-object, properties-not-object, required-not-list, required non-string member, malformed/unsupported array items are rejected with the tool name. |
| Precondition | 6 malformed cases. |
| Steps | 1. For each, call `MapTools`. |
| Expected | Error returned containing `myTool`. |
| Actual | All 6 rejected; tool name present. |
| Status | ✅ PASS |

## TEST-00033: Non-string enum member rejected (mapping negative)

| Field | Value |
|---|---|
| Target | FEAT-00004 - enum validation (plan risk) |
| Type | Unit |
| Objective | An enum containing a non-string member (`fast`, 42, `slow`) is rejected with tool + field path. |
| Precondition | `mode` enum `["fast", 42, "slow"]`. |
| Steps | 1. Call `MapTools`. |
| Expected | Error containing `myTool` and `mode`. |
| Actual | Error includes tool and `mode` path. |
| Status | ✅ PASS |

## TEST-00034: Duplicate tool names rejected (mapping negative)

| Field | Value |
|---|---|
| Target | FEAT-00004 - duplicate detection |
| Type | Unit |
| Objective | Two tools with the same name are rejected with `ErrDuplicateName`, not silently overwritten. |
| Precondition | Two identical `dup` tools. |
| Steps | 1. Call `MapTools`. |
| Expected | `errors.Is(ErrDuplicateName)`; message contains `dup`. |
| Actual | ErrDuplicateName returned; `dup` present. |
| Status | ✅ PASS |

## TEST-00035: Cyclic schema guarded (mapping boundary)

| Field | Value |
|---|---|
| Target | FEAT-00004 - recursion guard |
| Type | Unit |
| Objective | A self-referencing cyclic schema fails with an error, not a stack overflow. |
| Precondition | `object` `node` with property `self → node` (cycle). |
| Steps | 1. Call `MapTools`. |
| Expected | Error returned containing `cyc`. |
| Actual | Error returned; no overflow. |
| Status | ✅ PASS |

## TEST-00036: Over-deep nesting guarded (mapping boundary)

| Field | Value |
|---|---|
| Target | FEAT-00004 - recursion depth limit |
| Type | Unit |
| Objective | Nesting beyond `maxRecursionDepth` (64) is rejected. |
| Precondition | 300-level nested object schema. |
| Steps | 1. Call `MapTools`. |
| Expected | Error returned (recursion limit). |
| Actual | Error returned. |
| Status | ✅ PASS |
# Test Cases - FEAT-00007

> Headless CLI prompt input and final-response output (`cmd/server`).
> Derived from `artifacts/superpowers/brainstorm.md` AC1-AC5 + approved `artifacts/superpowers/plan.md`.
> Status: ✅ Pass · ❌ Fail · ⏭️ Skip. Execution evidence in `tests/REPORTS.md`.

## TEST-00065: Non-empty stdin prompt runs the loop and writes only final text (AC2)

| Field | Value |
|---|---|
| Target | FEAT-00007 - stdin prompt input (AC2) |
| Type | Unit + CLI |
| Objective | When `--prompt` is omitted and stdin (piped/non-TTY) is non-empty, the agent-loop path is taken and the final model text is written to stdout. |
| Precondition | Valid env; stdin reader injected as non-terminal. |
| Steps | 1. `run(out, strings.NewReader("list files"))` with unresolvable MCP command. 2. Assert the loop path is taken (MCP start failure) and no bootstrap output is written. |
| Expected | No bootstrap output; error names the MCP start failure; loop path proven. |
| Actual | `start MCP client: ...` error; no bootstrap on stdout. |
| Status | ✅ PASS |

## TEST-00066: Empty stdin with no --prompt prints bootstrap, exit 0 (AC3)

| Field | Value |
|---|---|
| Target | FEAT-00007 - bootstrap fallback (AC3) |
| Type | Unit + CLI |
| Objective | Empty/absent stdin (EOF with no bytes) and no `--prompt` preserves the existing bootstrap contract with exit 0. |
| Precondition | Valid env; empty stdin reader. |
| Steps | 1. `run(out, strings.NewReader(""))`. 2. Compare output to the exact bootstrap line. 3. CLI: pipe `""` to the binary, check exit code. |
| Expected | Exact bootstrap line; exit 0. |
| Actual | `MCP Host bootstrap complete. Integrations are not configured or started.`; exit=0. |
| Status | ✅ PASS |

## TEST-00067: --prompt flag still runs the loop directly (AC1)

| Field | Value |
|---|---|
| Target | FEAT-00007 - --prompt flag (AC1) |
| Type | Unit |
| Objective | `--prompt "X"` is used verbatim; stdin is not consulted on that path. |
| Precondition | Valid env. |
| Steps | 1. `run(out, stdin, "--prompt", "X")`. 2. Assert flag parsing and loop dispatch still work (regression; covered by existing dispatch in `run`). |
| Expected | Flag path unchanged; final text path reached. |
| Actual | Flag path unchanged; covered by existing tests + build. |
| Status | ✅ PASS |

## TEST-00068: Prompt trimmed of surrounding whitespace (boundary)

| Field | Value |
|---|---|
| Target | FEAT-00007 - input normalization |
| Type | Unit |
| Objective | Leading/trailing whitespace (incl. trailing newline from `echo`) is trimmed before dispatch. |
| Precondition | stdin reader contains `"  list files\n"`. |
| Steps | 1. `readPromptFromStdin(strings.NewReader("  list files\n"))`. |
| Expected | Returns `"list files"`. |
| Actual | `"list files"` returned. |
| Status | ✅ PASS |

## TEST-00069: Stdin read error is surfaced, never silent (AC4 / error-handling)

| Field | Value |
|---|---|
| Target | FEAT-00007 - error handling (AC4) |
| Type | Unit |
| Objective | A stdin read failure returns a wrapped, descriptive error (no silent fallback to bootstrap). |
| Precondition | Reader returns error on first read. |
| Steps | 1. `readPromptFromStdin(errReader{})`. 2. Assert non-nil error. |
| Expected | Non-nil error surfaced. |
| Actual | Error surfaced. |
| Status | ✅ PASS |

## TEST-00070: Failures write to stderr and exit non-zero, no secret leak (AC4)

| Field | Value |
|---|---|
| Target | FEAT-00007 - diagnostics + exit code (AC4) |
| Type | CLI (E2E) |
| Objective | On failure the binary writes a concise diagnostic to stderr and exits non-zero; stdout carries no diagnostics and no secret is leaked. |
| Precondition | Valid env; piped prompt; unresolvable MCP command. |
| Steps | 1. Pipe `"list files"` to built binary. 2. Capture exit code + stderr. 3. Grep for any sentinel secret. |
| Expected | Exit=1; diagnostic on stderr; no secret; bootstrap absent from stdout. |
| Actual | exit=1; `start MCP client: ...` on stderr; no secret. |
| Status | ✅ PASS |

## TEST-00071: Regression - bootstrap, secret-leak, flag-error tests unchanged

| Field | Value |
|---|---|
| Target | FEAT-00007 - existing contract |
| Type | Unit (regression) |
| Objective | Pre-existing tests (`TestRun_WritesSafeBootstrapStatus`, `TestRun_SmokeUnconfiguredInvocationEndsCleanly`, `TestRun_InvalidConfigDoesNotLeakSecret`, `TestRun_FlagParseErrorReturnsCleanError`) still pass with the new `run` signature. |
| Precondition | Existing test suite. |
| Steps | 1. `go test ./cmd/server -count=1`. |
| Expected | All pass. |
| Actual | All pass. |
| Status | ✅ PASS |

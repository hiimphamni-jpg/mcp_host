# Test Cases - FEAT-00002

> Config validation (`internal/config`) + Filesystem MCP process policy (`internal/policy`).
> Derived from `docs/business-logic.md` §4/§5 + `docs/tdd/MCP_HOST_TDD.md` §4, §8.
> Status: ✅ Pass · ❌ Fail · ⏭️ Skip. Execution evidence in `tests/REPORTS.md`.

## TEST-00004: Valid full config loads and values parse correctly (config)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/config` |
| Type | Unit |
| Objective | Positive path: a complete, valid environment parses every typed field. |
| Precondition | Standard Go env set via test helper (API key, model, command, args JSON, roots, `MCP_TIMEOUT=30s`, `LLM_TIMEOUT=45s`, `AGENT_MAX_ITERATIONS=10`, `MCP_MAX_RESULT_BYTES=65536`). |
| Steps | 1. Call `config.Load()`. |
| Expected | No error; `MCPTimeout=30s`, `LLMTimeout=45s`, `MaxAgentIterations=10`, `MaxResultBytes=65536`, args length 2. |
| Actual | No error; all typed fields parsed correctly. |
| Status | ✅ PASS |

## TEST-00005: Missing required field rejected (config)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/config` validation |
| Type | Unit |
| Objective | Negative: omitting a required variable fails load with a named field error. |
| Precondition | Valid env, then unset `GEMINI_API_KEY`. |
| Steps | 1. Call `config.Load()`. |
| Expected | Error returned, and error text mentions `GEMINI_API_KEY`; no silent acceptance. |
| Actual | Error returned; message names `GEMINI_API_KEY`. |
| Status | ✅ PASS |

## TEST-00006: Malformed `MCP_FILESYSTEM_ARGS_JSON` rejected (config)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/config` argv parsing |
| Type | Unit |
| Objective | Negative: non-parseable JSON argv produces a field-level error, not a panic. |
| Precondition | Valid env with `MCP_FILESYSTEM_ARGS_JSON='["unterminated'`. |
| Steps | 1. Call `config.Load()`. |
| Expected | Error returned mentioning `MCP_FILESYSTEM_ARGS_JSON`. |
| Actual | Error returned; names `MCP_FILESYSTEM_ARGS_JSON`. |
| Status | ✅ PASS |

## TEST-00007: Zero / non-positive limits rejected (config boundary)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/config` bounds |
| Type | Unit |
| Objective | Boundary: `0` for timeouts/iterations/bytes must be invalid (positive required). |
| Precondition | Valid env with `AGENT_MAX_ITERATIONS=0` (repeat for `MCP_TIMEOUT=0`, `LLM_TIMEOUT=0`, `MCP_MAX_RESULT_BYTES=0`). |
| Steps | 1. Call `config.Load()`. |
| Expected | Error returned naming the offending variable. |
| Actual | Error returned for `AGENT_MAX_ITERATIONS=0`. |
| Status | ✅ PASS |

## TEST-00008: Wrong-type numeric / duration values rejected (config)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/config` parsing |
| Type | Unit |
| Objective | Negative: non-integer `MCP_MAX_RESULT_BYTES` and non-duration `MCP_TIMEOUT` are rejected. |
| Precondition | Valid env with `MCP_MAX_RESULT_BYTES='not-an-int'` (and separately `MCP_TIMEOUT='not-a-duration'`). |
| Steps | 1. Call `config.Load()`. |
| Expected | Error naming the invalid variable. |
| Actual | Errors name the invalid variable in both cases. |
| Status | ✅ PASS |

## TEST-00009: Config error never leaks the API key (config security)

| Field | Value |
|---|---|
| Target | FEAT-00002 - secret-safe errors |
| Type | Unit |
| Objective | Verify the aggregate error must not include the `GEMINI_API_KEY` value. |
| Precondition | Valid env with `GEMINI_API_KEY='supersecret-abc123'`, then clear `MCP_FILESYSTEM_COMMAND` to force an error. |
| Steps | 1. Call `config.Load()`. 2. Inspect `err.Error()`. |
| Expected | Error text must NOT contain `supersecret-abc123`. |
| Actual | Error text absent of secret; `TestConfigLoad_SecretNotLeaked` passes. |
| Status | ✅ PASS |

## TEST-00010: Default `HOST_LOG_LEVEL` applied (config boundary)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/config` optional field |
| Type | Unit |
| Objective | Boundary: omitted `HOST_LOG_LEVEL` defaults to `info`. |
| Precondition | Valid env without `HOST_LOG_LEVEL`. |
| Steps | 1. Call `config.Load()`. |
| Expected | `LogLevel == "info"`. |
| Actual | `LogLevel == "info"`. |
| Status | ✅ PASS |

## TEST-00011: Bare valid executable accepted (policy positive)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` allowlist |
| Type | Unit |
| Objective | Positive: a command without separators/metachars is accepted; args are preserved. |
| Precondition | `policy.New("npx", ["-y","@modelcontextprotocol/server-filesystem"], [tempdir])`. |
| Steps | 1. Call `policy.New`. |
| Expected | No error; `Command()=="npx"`, `Args()` length 2. |
| Actual | No error; `Command()=="npx"`, `Args()` length 2. |
| Status | ✅ PASS |

## TEST-00012: Unsafe / non-bare command rejected (policy negative)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` command allowlist |
| Type | Unit |
| Objective | Negative: path separators, shell metacharacters (`/ \ | ; & $ ( ) whitespace`), or embedded args must be rejected. |
| Precondition | Cases: `/usr/bin/npx`, `C:\bin\npx.exe`, `npx -y`, `npx;rm -rf /`, `npx|sh`, `npx&&sh`, `npx$(sh)`, `npx\bin`. |
| Steps | 1. For each value, call `policy.New(cmd, ["-y"], roots)`. |
| Expected | All return an error. |
| Actual | All 8 injection cases rejected. |
| Status | ✅ PASS |

## TEST-00013: Empty command rejected (policy boundary)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` allowlist |
| Type | Unit |
| Objective | Boundary: empty command string rejected. |
| Precondition | `policy.New("", ["-y"], [tempdir])`. |
| Steps | 1. Call `policy.New`. |
| Expected | Error. |
| Actual | Error returned. |
| Status | ✅ PASS |

## TEST-00014: Path inside canonical root allowed (policy positive)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` containment |
| Type | Unit |
| Objective | Positive: a file directly under the allowed root is `Contains==true`; roots canonicalized to absolute. |
| Precondition | Root = `t.TempDir()`; file at `<root>/README.md`. |
| Steps | 1. `p := New(...root...)`. 2. `p.Contains(file)`. |
| Expected | `true`, no error. |
| Actual | `true`, no error; roots absolute. |
| Status | ✅ PASS |

## TEST-00015: Path outside root denied (policy negative)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` containment |
| Type | Unit |
| Objective | Negative: a directory outside the allowed root must be denied. |
| Precondition | Root = `t.TempDir()`, other = separate `t.TempDir()`. |
| Steps | 1. `New(...)`. 2. `Contains(other)`. |
| Expected | `false`, no error. |
| Actual | `false`. |
| Status | ✅ PASS |

## TEST-00016: Parent traversal denied (policy boundary)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` containment |
| Type | Unit |
| Objective | Boundary: `../..` traversal escaping the root must be denied. |
| Precondition | Root = `t.TempDir()`. |
| Steps | 1. `Contains(<root>/../../etc)`. |
| Expected | `false`. |
| Actual | `false`. |
| Status | ✅ PASS |

## TEST-00017: `ParseArgs` valid JSON array (policy positive)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` argv contract |
| Type | Unit |
| Objective | Positive: a JSON array of strings decodes to the expected slice. |
| Precondition | `raw = '["-y","@modelcontextprotocol/server-filesystem"]'`. |
| Steps | 1. `policy.ParseArgs(raw)`. |
| Expected | No error; 2 elements; `[0]=="-y"`. |
| Actual | No error; 2 elements; `[0]=="-y"`. |
| Status | ✅ PASS |

## TEST-00018: `ParseArgs` rejects non-string / malformed (policy negative)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `internal/policy` arg contract |
| Type | Unit |
| Objective | Negative: non-string array element, bare string, or invalid JSON all fail. |
| Precondition | `'["a", 42]'`, `'"not-an-array"'`, `'{'`. |
| Steps | 1. For each, call `policy.ParseArgs`. |
| Expected | All return an error. |
| Actual | All 3 malformed cases rejected. |
| Status | ✅ PASS |

## TEST-00019: Invalid config fails fast at CLI, no MCP spawn (E2E)

| Field | Value |
|---|---|
| Target | FEAT-00002 - `cmd/server` composition, TDD §4 |
| Type | E2E |
| Objective | Given missing `GEMINI_API_KEY`, `go run ./cmd/server` exits non-zero with a user-safe diagnostic and does not spawn MCP. |
| Precondition | Environment without `GEMINI_API_KEY`. |
| Steps | 1. `go run ./cmd/server`. |
| Expected | Exit code != 0; stderr contains a concise `invalid configuration` message; no MCP process started; no secret leak. |
| Actual | Exit 1; `invalid configuration: GEMINI_API_KEY is required ...`; sentinel secret absent; no MCP spawned. Valid config exits 0 with bootstrap output preserved. |
| Status | ✅ PASS |
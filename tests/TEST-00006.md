# Test Cases - FEAT-00006

> Bounded agentic loop, tool-result context, cancellation, error handling (`internal/agent`).
> Derived from `docs/business-logic.md` ﾂｧ3窶・ + `docs/tdd/MCP_HOST_TDD.md` ﾂｧ7 + ADR-0001.
> Status: 笨・Pass ﾂｷ 笶・Fail ﾂｷ 竢ｭ・・Skip. Execution evidence in `tests/REPORTS.md`.

## TEST-00051: Final text returned directly (agent positive)

| Field | Value |
|---|---|
| Target | FEAT-00006 - Run loop (AC3) |
| Type | Unit |
| Objective | When the LLM returns text with no tool calls, `Run` returns that text immediately. |
| Precondition | `stubLLM` returns `Response{Text:"done"}` on first call. |
| Steps | 1. `agent.New(valid opts)`. 2. `Run(ctx, "hi")`. |
| Expected | Return `"done"`, nil error; LLM called once. |
| Actual | `"done"` returned. |
| Status | 笨・PASS |

## TEST-00051b: Single tool call then final text (agent AC3)
>
> **Note:** TEST-00051b is a continuation case paired with TEST-00051; it is the
> same scenario numbered separately for traceability in `tests/REPORTS.md`.

| Field | Value |
|---|---|
| Target | FEAT-00006 - agent loop |
| Type | Unit |
| Objective | LLM requests one tool; tool executes; result fed back; LLM returns final text. |
| Precondition | stubLLM returns tool call, then text; stubMCP returns a result. |
| Steps | 1. Run. 2. Assert tool called once with args; 3. Assert final text. |
| Expected | Tool executes; final text returned; no error. |
| Actual | Loop completed correctly. |
| Status | 笨・PASS |

## TEST-00052: Multiple tool calls in one turn (agent)

| Field | Value |
|---|---|
| Target | FEAT-00006 - sequential execution (ADR-0001 D4) |
| Type | Unit |
| Objective | Multiple tool calls from one turn are executed sequentially in request order. |
| Precondition | stubLLM returns 2 tool calls, then final text. |
| Steps | 1. Run. 2. Assert stubMCP called for both, in order. |
| Expected | Both tools executed in order. |
| Actual | Sequential execution confirmed. |
| Status | 笨・PASS |

## TEST-00053: Iteration limit returns ErrIterationLimit (agent boundary)

| Field | Value |
|---|---|
| Target | FEAT-00006 - AGENT_MAX_ITERATIONS |
| Type | Unit |
| Objective | A loop that never reaches final text hits `ErrIterationLimit`, not infinite. |
| Precondition | stubLLM always returns a tool call; MaxIterations=3. |
| Steps | 1. Run. |
| Expected | Error `errors.Is(ErrIterationLimit)`. |
| Actual | ErrIterationLimit returned. |
| Status | 笨・PASS |

## TEST-00054: Unknown tool becomes safe result (agent negative)

| Field | Value |
|---|---|
| Target | FEAT-00006 - unauthorized tool (business-logic ﾂｧ5) |
| Type | Unit |
| Objective | LLM requests a tool not in the registry 竊・bounded `action unavailable` result, MCP not called. |
| Precondition | stubLLM requests `ghost_tool`; registry lacks it. |
| Steps | 1. Run. |
| Expected | stubMCP NOT called; result is `action unavailable`. |
| Actual | Tool not called; safe result. |
| Status | 笨・PASS |

## TEST-00055: Invalid args become safe result (agent negative)

| Field | Value |
|---|---|
| Target | FEAT-00006 - schema validation (business-logic ﾂｧ5) |
| Type | Unit |
| Objective | Schema-invalid arguments 竊・bounded `arguments failed validation`, MCP not called. |
| Precondition | Registry has tool needing required `id`; LLM omits it. |
| Steps | 1. Run. |
| Expected | stubMCP NOT called; result `arguments failed validation`. |
| Status | 笨・PASS |

## TEST-00056: MCP failure becomes bounded result, loop continues (AC4)

| Field | Value |
|---|---|
| Target | FEAT-00006 - safe failure |
| Type | Unit |
| Objective | A tool-call error maps to a bounded, non-secret result; the host does not crash and the loop continues. |
| Precondition | stubMCP returns an error; stubLLM then returns final text. |
| Steps | 1. Run. |
| Expected | Final text returned; error surfaced only as bounded result, not propagated. |
| Actual | Loop continued to final text. |
| Status | 笨・PASS |

## TEST-00057: Cancellation propagates and terminates (agent boundary)

| Field | Value |
|---|---|
| Target | FEAT-00006 - caller cancellation (ADR-0001 D3) |
| Type | Unit |
| Objective | Cancelling the context stops the loop; the cancelled ctx reaches MCP `CallTool`. |
| Precondition | Cancelled ctx; stubMCP asserts ctx.Err(). |
| Steps | 1. Run with cancelled ctx. |
| Expected | Returns `context.Canceled`; tool sees cancelled ctx; no further turns. |
| Actual | Cancellation propagated and terminal. |
| Status | 笨・PASS |

## TEST-00058: Cancellation mid-loop overrides bounded tool error (D3)

| Field | Value |
|---|---|
| Target | FEAT-00006 - precedence |
| Type | Unit |
| Objective | If the context is cancelled during a tool error, cancellation wins over the bounded-result path. |
| Precondition | Cancel ctx; stubMCP returns error AND ctx cancelled. |
| Steps | 1. Run. |
| Expected | Returns `context.Canceled` (not a bounded result). |
| Actual | Cancellation precedence confirmed. |
| Status | 笨・PASS |

## TEST-00059: Result truncated with explicit marker (agent boundary)

| Field | Value |
|---|---|
| Target | FEAT-00006 - MCP_MAX_RESULT_BYTES (TDD ﾂｧ7) |
| Type | Unit |
| Objective | A tool result larger than the byte cap is truncated with `...[truncated]`. |
| Precondition | Tool returns large response; `MaxResultBytes` small. |
| Steps | 1. Run. 2. Inspect history tool result content. |
| Expected | Content ends with truncation marker and 竕､ cap. |
| Actual | Truncation marker present. |
| Status | 笨・PASS |

## TEST-00060: Secret content not in output (agent security)

| Field | Value |
|---|---|
| Target | FEAT-00006 - secret redaction |
| Type | Unit |
| Objective | A secret/path in a tool response is never surfaced to the user (only bounded, non-secret context). |
| Precondition | Tool response contains a sentinel; `ErrorResult`/`classifyGenerateError` used. |
| Steps | 1. Run with error/secret path. |
| Expected | Output contains no sentinel/raw path. |
| Actual | No secret leaked. |
| Status | 笨・PASS |

## TEST-00061: Argument validator 窶・happy path (validator positive)

| Field | Value |
|---|---|
| Target | FEAT-00006 - schema validation |
| Type | Unit |
| Objective | Valid args matching the schema validate cleanly. |
| Precondition | Schema with string/integer/required; valid args. |
| Steps | 1. `validateArgs`. |
| Expected | nil error. |
| Actual | nil. |
| Status | 笨・PASS |

## TEST-00062: Validator negative + boundary cases (validator)

| Field | Value |
|---|---|
| Target | FEAT-00006 - schema validation |
| Type | Unit |
| Objective | Missing required, wrong type, enum mismatch, nested object, array items, empty/no-arg schema are all handled. |
| Precondition | Table of invalid inputs. |
| Steps | 1. `validateArgs` per case. |
| Expected | Errors for invalid; pass for empty/no-arg; numeric kinds & JSON-decoded arrays work. |
| Status | 笨・PASS |

## TEST-00063: Options validation (constructor boundary)

| Field | Value |
|---|---|
| Target | FEAT-00006 - construction |
| Type | Unit |
| Objective | `New` rejects nil LLM/Tools and non-positive limits; accepts valid options. |
| Precondition | Nil / invalid opts. |
| Steps | 1. `New(opts)`. |
| Expected | Error for invalid; nil for valid. |
| Status | 笨・PASS |

## TEST-00064: Errors are user-safe and unwrap to sentinels (agent security)

| Field | Value |
|---|---|
| Target | FEAT-00006 - error hygiene |
| Type | Unit |
| Objective | `agent.Error` message never embeds the cause (no path/secret); `errors.Is` matches the sentinel. |
| Precondition | Wrap a cause with secret string. |
| Steps | 1. Assert `errors.Is`; 2. Assert message omits cause. |
| Expected | Sentinel matches; no secret in message. |
| Status | 笨・PASS |

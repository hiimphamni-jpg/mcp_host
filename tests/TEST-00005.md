# Test Cases - FEAT-00005

> Gemini provider adapter and conversation request/response mapping (`internal/llm`).
> Derived from `docs/business-logic.md` §3/§5 + `docs/tdd/MCP_HOST_TDD.md` §2.1, AC5.
> Status: ✅ Pass · ❌ Fail · ⏭️ Skip. Execution evidence in `tests/REPORTS.md`.

## TEST-00037: User text message → genai content (conversion positive)

| Field | Value |
|---|---|
| Target | FEAT-00005 - neutral→genai conversion |
| Type | Unit |
| Objective | A user text Message converts to a genai.Content with role `user` and a text part. |
| Precondition | `Message{Role: RoleUser, Text: "hello"}`. |
| Steps | 1. `toGemContent(msg)`. |
| Expected | Content role `user`; 1 text part with the text. |
| Actual | Converted correctly. |
| Status | ✅ PASS |

## TEST-00038: Model tool call → FunctionCall part (conversion positive)

| Field | Value |
|---|---|
| Target | FEAT-00005 - tool-call mapping |
| Type | Unit |
| Objective | A model ToolCall becomes a genai FunctionCall part with name + arguments. |
| Precondition | `Message{Role: RoleModel, ToolCalls: [{Name:"echo", Arguments:{message:"hi"}}]}`. |
| Steps | 1. `toGemContent(msg)`. |
| Expected | FunctionCall part with matching name/args. |
| Actual | Mapped correctly. |
| Status | ✅ PASS |

## TEST-00039: User tool result → FunctionResponse part (conversion positive)

| Field | Value |
|---|---|
| Target | FEAT-00005 - tool-result mapping |
| Type | Unit |
| Objective | A ToolResult becomes a genai FunctionResponse part (user role). |
| Precondition | `Message{Role: RoleUser, ToolResults: [{Name:"echo", Response:{ok:true}}]}`. |
| Steps | 1. `toGemContent(msg)`. |
| Expected | FunctionResponse part with matching name/response. |
| Actual | Mapped correctly. |
| Status | ✅ PASS |

## TEST-00040: Mixed parts preserved in order (conversion boundary)

| Field | Value |
|---|---|
| Target | FEAT-00005 - multi-part message |
| Type | Unit |
| Objective | A message with text + tool calls + tool results carries all parts in order. |
| Precondition | Message with text, 1 ToolCall, 1 ToolResult. |
| Steps | 1. `toGemContent(msg)`. |
| Expected | 3 parts: text, FunctionCall, FunctionResponse (order preserved). |
| Actual | All parts present in order. |
| Status | ✅ PASS |

## TEST-00041: Response text extracted (response positive)

| Field | Value |
|---|---|
| Target | FEAT-00005 - response mapping |
| Type | Unit |
| Objective | A provider response with only text yields `Response{Text: ...}` and no ToolCalls. |
| Precondition | Fake genai response with text candidate. |
| Steps | 1. `responseToResponse(resp)`. |
| Expected | `Text` populated; `ToolCalls` empty. |
| Actual | Text extracted. |
| Status | ✅ PASS |

## TEST-00042: Function calls surfaced as ToolCalls (response boundary)

| Field | Value |
|---|---|
| Target | FEAT-00005 - function-call extraction |
| Type | Unit |
| Objective | FunctionCall parts become neutral ToolCalls with name + args. |
| Precondition | Fake response with 1 FunctionCall. |
| Steps | 1. `responseToResponse(resp)`. |
| Expected | 1 ToolCall with matching name/args. |
| Actual | ToolCall extracted. |
| Status | ✅ PASS |

## TEST-00043: Nil / empty response handled (response negative)

| Field | Value |
|---|---|
| Target | FEAT-00005 - null safety |
| Type | Unit |
| Objective | Nil or empty-candidate responses return an empty Response, not a panic. |
| Precondition | `nil`; empty candidates. |
| Steps | 1. `responseToResponse(nil)`. 2. `responseToResponse(&genai.GenerateContentResponse{})`. |
| Expected | `&Response{}` both times; no panic. |
| Actual | Empty Response; no panic. |
| Status | ✅ PASS |

## TEST-00044: Generate happy path — text only (adapter positive)

| Field | Value |
|---|---|
| Target | FEAT-00005 - `Gemini.Generate` |
| Type | Unit |
| Objective | A fake generator returning text produces a neutral Response with that text. |
| Precondition | Fake `modelsClient` returning text response. |
| Steps | 1. `Generate(ctx, Request{Contents:[user msg]})`. |
| Expected | No error; Response text matches. |
| Actual | Text returned. |
| Status | ✅ PASS |

## TEST-00045: Generate surfaces function calls (adapter boundary)

| Field | Value |
|---|---|
| Target | FEAT-00005 - tool-call surfacing |
| Type | Unit |
| Objective | Fake generator returning a FunctionCall yields neutral ToolCalls. |
| Precondition | Fake returns FunctionCall part. |
| Steps | 1. `Generate`. |
| Expected | `Response.ToolCalls` populated with name/args. |
| Actual | ToolCall surfaced. |
| Status | ✅ PASS |

## TEST-00046: Deadline → ErrTimeout (adapter negative)

| Field | Value |
|---|---|
| Target | FEAT-00005 - timeout mapping |
| Type | Unit |
| Objective | Provider `context.DeadlineExceeded` maps to typed `ErrTimeout` (LLM_TIMEOUT enforced). |
| Precondition | Fake returns `context.DeadlineExceeded`. |
| Steps | 1. `Generate`. |
| Expected | `errors.Is(err, ErrTimeout)`; message names `Generate`; no provider detail leaked. |
| Actual | ErrTimeout returned. |
| Status | ✅ PASS |

## TEST-00047: Caller cancel passes through (adapter negative)

| Field | Value |
|---|---|
| Target | FEAT-00005 - cancellation semantics |
| Type | Unit |
| Objective | Caller cancellation (`context.Canceled`) passes through unchanged — NOT converted to ErrTimeout. |
| Precondition | Fake returns `context.Canceled`. |
| Steps | 1. `Generate`. |
| Expected | `errors.Is(err, context.Canceled)`; NOT `ErrTimeout`. |
| Actual | Canceled passed through. |
| Status | ✅ PASS |

## TEST-00048: Provider error → ErrProvider (adapter negative)

| Field | Value |
|---|---|
| Target | FEAT-00005 - provider failure |
| Type | Unit |
| Objective | A non-timeout provider error maps to typed `ErrProvider` with user-safe message. |
| Precondition | Fake returns a generic error. |
| Steps | 1. `Generate`. |
| Expected | `errors.Is(err, ErrProvider)`; message contains no underlying detail. |
| Actual | ErrProvider returned. |
| Status | ✅ PASS |

## TEST-00049: Tool config built only for named tools (adapter boundary)

| Field | Value |
|---|---|
| Target | FEAT-00005 - tool declaration wiring |
| Type | Unit |
| Objective | Named tools map to prepared declarations in request order; no names → no tool config. |
| Precondition | decls map has `echo`/`ls`; requests with and without ToolNames. |
| Steps | 1. Generate with ToolNames `["echo","ls"]`. 2. Generate with empty names. |
| Expected | Config.Tools has both (in order); empty case has no Tools. |
| Actual | Both behaviors confirmed. |
| Status | ✅ PASS |

## TEST-00050: Contents built for request (adapter positive)

| Field | Value |
|---|---|
| Target | FEAT-00005 - request conversion |
| Type | Unit |
| Objective | All request Contents convert to genai.Content in order and reach the generator. |
| Precondition | 2 Messages (user text, model tool call). |
| Steps | 1. Generate; fake captures contents. |
| Expected | Fake received 2 contents in order. |
| Actual | Contents built and passed. |
| Status | ✅ PASS |
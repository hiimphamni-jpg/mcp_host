---
description: Start the end-to-end new-feature delivery flow.
agent: general
---

Coordinate Flow 1 (New Feature) in `.gemini/PROJECT_FLOWS.md` for this request: $ARGUMENTS

Start with `/think` behavior: clarify only information that is essential, record the brainstorm artifact, and identify whether architecture design is required. Do not write implementation code until a detailed `/plan` is approved. At every phase, state the task ID, current phase, required exit condition, and next slash command.

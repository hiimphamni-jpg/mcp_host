---
description: Start the bug-fix flow with reproduction and regression protection.
agent: general
---

Coordinate Flow 2 (Bug Fix) in `.gemini/PROJECT_FLOWS.md` for this symptom: $ARGUMENTS

Begin with the `/fix` protocol and create the debug artifact. Require a `BUG-xxxxx` ID, an approved plan, and a regression test where feasible before implementation. At every phase, state the task ID, current phase, required exit condition, and next slash command.
